public class anagrams_scratch
{
    public static void main(String[] args)
    {
        System.out.println("Clint Eastwood and Old West Action: ");
    }

    public static boolean anagrams(String string1, String strin2)
    {
        // sort the two strings manually
        if(string1.length() != string2.length())
        {
            return false;
        }

        // count letter frequencies of both strings, then match based on that
        countFlag = false;
        char[] charTable = new char[26];
        

        return countFlag;
    }
}